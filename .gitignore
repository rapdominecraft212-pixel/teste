skills/
node_modules/
upload/
tool-results/

# Cópia local persistente do Kwai-Editor para testes do agente GLM.
# Não é versionada no git — serve como backup local que sobrevive a
# `kwai_bridge.py setup` (que faz git reset --hard origin/main).
# Para restaurar a cópia: cp -r Kwai-Editor kwai-editor-test
kwai-editor-test/
